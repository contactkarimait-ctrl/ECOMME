/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/ECOMME',
  assetPrefix: '/ECOMME/',
}

export default nextConfig
